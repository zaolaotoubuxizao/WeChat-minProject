<view class="tabs">
    <view wx:for="{{tabsData}}"
    wx:key="id"
    class="{{item.isActive?'active':''}}"
    data-id="{{item.id}}"
    bindtap="handleChange"
    >
        {{item.title}}
    </view>     
</view>
  